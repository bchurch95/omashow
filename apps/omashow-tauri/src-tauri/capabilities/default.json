{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Editor and audience windows: core APIs, cross-window event sync, file dialogs",
  "windows": ["main", "audience"],
  "permissions": ["core:default", "dialog:default"]
}
